{
  "name": "2bi-site",
  "version": "1.0.0",
  "private": true,
  "description": "2BI — Beyond Business Intelligence. Sitio estático multi-página servido con `serve`.",
  "scripts": {
    "start": "serve scorecard-General/PAGINA-2BI -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard --no-port-switching"
  },
  "dependencies": {
    "serve": "14.2.4"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
